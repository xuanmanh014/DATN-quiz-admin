import { Layout } from 'antd'

const LayoutHeader = () => {
    return (
        <Layout.Header style={{ background: "#fff" }}>
            Header
        </Layout.Header>
    )
}

export default LayoutHeader